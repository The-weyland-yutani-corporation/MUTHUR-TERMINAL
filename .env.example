# ── MU/TH/UR 6000 Terminal — Environment Configuration ──

# ── Azure AI Foundry (BYOK) — Recommended for deployment ──
# Deploy a Claude model in Azure AI Foundry, then set these:

# Your Azure AI Foundry endpoint URL
# e.g. https://your-resource.openai.azure.com/openai/v1/
LLM_BASE_URL=

# Azure API key for your Foundry deployment
LLM_API_KEY=

# ── Node.js paths (local dev only) ──
# Required if your default node is < 22 (Copilot CLI needs node:sqlite)
# NODE_PATH=/path/to/node22+/bin/node
# CLI_PATH=/path/to/node_modules/@github/copilot/npm-loader.js
