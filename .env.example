# ── MU/TH/UR 6000 Terminal — Environment Configuration ──

# GitHub Personal Access Token (required for production)
# Generate at: https://github.com/settings/tokens
# Required scope: copilot
GITHUB_TOKEN=

# Copilot SDK model (default: claude-sonnet-4-5)
COPILOT_MODEL=claude-sonnet-4-5

# (Optional) Path to Node.js 22+ binary — required if your default node is < 22
# The Copilot CLI requires node:sqlite which needs Node 22+
# NODE_PATH=/path/to/node22+/bin/node

# (Optional) Path to Copilot CLI entry point — only needed if not on PATH
# CLI_PATH=/path/to/node_modules/@github/copilot/npm-loader.js
