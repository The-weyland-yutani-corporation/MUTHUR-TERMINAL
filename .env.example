# ── MU/TH/UR 6000 Terminal — Environment Configuration ──

# ── BYOK (Bring Your Own Key) — Recommended for deployment ──
# Set these to use your own LLM provider (no GitHub Copilot subscription needed)

# LLM provider: "anthropic" | "openai" | "azure" (default: anthropic)
LLM_PROVIDER=anthropic

# API key from your provider
LLM_API_KEY=

# Provider base URL (defaults auto-set per provider)
# Anthropic: https://api.anthropic.com/v1
# OpenAI:    https://api.openai.com/v1
# Azure:     https://your-resource.openai.azure.com
# LLM_BASE_URL=

# ── GitHub Copilot Auth (alternative to BYOK) ──
# If using Copilot subscription instead of BYOK, set this token
# Generate at: https://github.com/settings/tokens (scope: copilot)
# GITHUB_TOKEN=

# ── Node.js paths (local dev only) ──
# Required if your default node is < 22 (Copilot CLI needs node:sqlite)
# NODE_PATH=/path/to/node22+/bin/node
# CLI_PATH=/path/to/node_modules/@github/copilot/npm-loader.js
