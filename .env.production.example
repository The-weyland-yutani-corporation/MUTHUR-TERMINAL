# ── MU/TH/UR 6000 Terminal — Production Environment ──
# Copy to .env.production and fill in values.
# This file is for Azure Container Apps deployment.

# ── GitHub Copilot Auth (REQUIRED) ──
# Fine-grained PAT with "Copilot Requests" account permission
# Create at: https://github.com/settings/personal-access-tokens/new
# Also set as a Container Apps secret:
#   az containerapp secret set --name muthur-terminal --resource-group muthur-rg --secrets github-token=<YOUR_PAT>
GITHUB_TOKEN=

# ── Azure OIDC (GitHub Actions CI/CD — set as repo variables, not here) ──
# These are configured in GitHub repo Settings > Secrets and variables > Actions > Variables tab.
# They are NOT app env vars — they're used by the deploy workflow only.
#   AZURE_CLIENT_ID=<from az ad app create>
#   AZURE_TENANT_ID=<from az account show --query tenantId>
#   AZURE_SUBSCRIPTION_ID=<from az account show --query id>

# ── Next.js ──
NODE_ENV=production
NEXT_TELEMETRY_DISABLED=1
HOSTNAME=0.0.0.0
PORT=3000
