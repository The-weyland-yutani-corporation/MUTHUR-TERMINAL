name: Deploy to Azure Container Apps

# ── Trigger ──
# Staging:    push to 'staging' branch
# Production: push to 'main' branch
# Manual:     workflow_dispatch with environment selector
on:
  push:
    branches: [main, staging]
  workflow_dispatch:
    inputs:
      environment:
        description: "Target environment"
        required: true
        type: choice
        options:
          - staging
          - production

# ── Prerequisites (one-time Azure CLI setup, repeat per environment) ──
#
# 1. Create GitHub Environments: "staging" and "production"
#    (Settings > Environments > New environment)
#
# 2. Per environment, create Azure resources:
#      az group create --name <RG_NAME> --location eastus
#      az acr create --name <ACR_NAME> --resource-group <RG_NAME> --sku Basic --admin-enabled true
#      az containerapp env create --name <ENV_NAME> --resource-group <RG_NAME> --location eastus
#      az containerapp create --name <APP_NAME> --resource-group <RG_NAME> \
#        --environment <ENV_NAME> --image <ACR_NAME>.azurecr.io/muthur-terminal:latest \
#        --target-port 3000 --ingress external
#
# 3. Create Azure PostgreSQL Flexible Server (for conversation persistence):
#      az postgres flexible-server create --name <DB_NAME> --resource-group <RG_NAME> \
#        --location eastus --admin-user <ADMIN_USER> --admin-password <ADMIN_PASSWORD> \
#        --sku-name Standard_B1ms --tier Burstable --storage-size 32 \
#        --version 14 --public-access all
#      az postgres flexible-server db create --resource-group <RG_NAME> \
#        --server-name <DB_NAME> --database-name muthur
#
# 4. Set Container App secrets (run for each environment):
#      az containerapp secret set --name <APP_NAME> --resource-group <RG_NAME> \
#        --secrets \
#          github-token=<YOUR_FINE_GRAINED_PAT> \
#          database-url=<POSTGRESQL_CONNECTION_STRING> \
#          nextauth-secret=<RANDOM_SECRET_openssl_rand_base64_32> \
#          github-client-id=<GITHUB_OAUTH_CLIENT_ID> \
#          github-client-secret=<GITHUB_OAUTH_CLIENT_SECRET>
#
# 5. Create Azure AD app + federated credentials (one per branch):
#      az ad app create --display-name "muthur-github-actions"
#      az ad sp create --id <APP_ID>
#      az role assignment create --assignee <APP_ID> --role contributor \
#        --scope /subscriptions/<SUB_ID>/resourceGroups/<RG_NAME>
#      # Staging credential
#      az ad app federated-credential create --id <APP_ID> --parameters \
#        '{"name":"github-staging","issuer":"https://token.actions.githubusercontent.com",
#          "subject":"repo:<OWNER>/MUTHUR-Terminal:environment:staging",
#          "audiences":["api://AzureADTokenExchange"]}'
#      # Production credential
#      az ad app federated-credential create --id <APP_ID> --parameters \
#        '{"name":"github-production","issuer":"https://token.actions.githubusercontent.com",
#          "subject":"repo:<OWNER>/MUTHUR-Terminal:environment:production",
#          "audiences":["api://AzureADTokenExchange"]}'
#
# 6. In EACH GitHub Environment, set these variables:
#      AZURE_CLIENT_ID, AZURE_TENANT_ID, AZURE_SUBSCRIPTION_ID,
#      AZURE_CONTAINER_APP_NAME, AZURE_RESOURCE_GROUP, AZURE_CONTAINER_REGISTRY,
#      NEXTAUTH_URL (e.g., https://muthur-terminal.azurecontainerapps.io)
#
# 7. Create GitHub OAuth App (for user authentication):
#      Go to: https://github.com/settings/developers
#      Create new OAuth App with callback URL: <NEXTAUTH_URL>/api/auth/callback/github
#      Use the Client ID and Client Secret in step 4 above

env:
  IMAGE_NAME: muthur-terminal

jobs:
  # ── Job 1: Determine target environment ──
  resolve-env:
    runs-on: ubuntu-latest
    permissions: {}
    outputs:
      environment: ${{ steps.set.outputs.environment }}
    steps:
      - name: Resolve environment from trigger
        id: set
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            ENV="${{ inputs.environment }}"
          elif [ "${{ github.ref }}" = "refs/heads/main" ]; then
            ENV="production"
          elif [ "${{ github.ref }}" = "refs/heads/staging" ]; then
            ENV="staging"
          else
            echo "::error::Unsupported branch for deployment"
            exit 1
          fi
          echo "environment=$ENV" >> "$GITHUB_OUTPUT"

  # ── Job 2: Build & Deploy ──
  build-and-deploy:
    needs: resolve-env
    runs-on: ubuntu-latest
    environment: ${{ needs.resolve-env.outputs.environment }}
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Log in to Azure Container Registry
        run: az acr login --name ${{ vars.AZURE_CONTAINER_REGISTRY }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ vars.AZURE_CONTAINER_REGISTRY }}.azurecr.io/${{ env.IMAGE_NAME }}:${{ github.sha }}
            ${{ vars.AZURE_CONTAINER_REGISTRY }}.azurecr.io/${{ env.IMAGE_NAME }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Deploy to Azure Container Apps
        uses: azure/container-apps-deploy-action@v2
        with:
          resourceGroup: ${{ vars.AZURE_RESOURCE_GROUP }}
          containerAppName: ${{ vars.AZURE_CONTAINER_APP_NAME }}
          imageToDeploy: ${{ vars.AZURE_CONTAINER_REGISTRY }}.azurecr.io/${{ env.IMAGE_NAME }}:${{ github.sha }}
          environmentVariables: >-
            GITHUB_TOKEN=secretref:github-token
            DATABASE_URL=secretref:database-url
            NEXTAUTH_SECRET=secretref:nextauth-secret
            GITHUB_CLIENT_ID=secretref:github-client-id
            GITHUB_CLIENT_SECRET=secretref:github-client-secret
            NEXTAUTH_URL=${{ vars.NEXTAUTH_URL }}
